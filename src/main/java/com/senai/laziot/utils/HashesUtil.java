package com.senai.laziot.utils;

//import com.senai.laziot.user.UsuarioEntity;
//import org.springframework.beans.factory.annotation.Autowired;
//import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.stereotype.Component;

@Component
public class HashesUtil {
/*
    protected String secretCodeHashPassword = "@Laziot#Hash$1324";
    protected String secretCodeHashUniqueCode = "@Laziot#Hash$1324";
    protected String tempPassEncoded;

    @Autowired
    private PasswordEncoder passwordEncoder;

    public UsuarioEntity createPasswordHash(String senha){
        tempPassEncoded = passwordEncoder.encode(senha+secretCodeHashPassword);
        return new UsuarioEntity(tempPassEncoded, passwordEncoder.encode(tempPassEncoded+secretCodeHashUniqueCode));
    }
*/

}
